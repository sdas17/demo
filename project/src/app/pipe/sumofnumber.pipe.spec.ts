import { SumofnumberPipe } from './sumofnumber.pipe';

describe('SumofnumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SumofnumberPipe();
    expect(pipe).toBeTruthy();
  });
});
