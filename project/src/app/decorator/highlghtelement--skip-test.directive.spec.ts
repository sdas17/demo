import { HighlghtelementSkipTestDirective } from './highlghtelement--skip-test.directive';

describe('HighlghtelementSkipTestDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlghtelementSkipTestDirective();
    expect(directive).toBeTruthy();
  });
});
